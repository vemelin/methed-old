'use strict'

let newValue = prompt ('Введите строку', '');

const modifyString = (input) => {
    let value = input,
    currentString = 'привет Мир';
    console.log(currentString.push('asdfasdf'));
}

modifyString(newValue)